<h1>Products</h1>

<% cart.each do |item| %>
    <li><%= item %></li>
<% end %>

<%= form_tag({controller: "products", action: "add", method: "post"}) do %>
    <%= text_field_tag(:product, "Enter a product") %>
    <%= submit_tag("add to cart") %>
<% end %>